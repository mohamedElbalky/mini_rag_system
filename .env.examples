# Django settings
SECRET_KEY='SECRET_KEY_PLACEHOLDER'
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Redis settings for Channels
REDIS_HOST=redis
REDIS_PORT=6380

# Gemini API Key
GEMINI_API_KEY='GEMINI_API_KEY_PLACEHOLDER'
GEMINI_MODEL='GEMINI_MODEL_PLACEHOLDER' # e.g., 'gemini-2.5-flash'
GEMINI_EMBEDDING_MODEL='GEMINI_EMBEDDING_MODEL_PLACEHOLDER' # e.g., 'text-embedding-004'
GEMINI_MAX_TOKENS=1000
EMBEDDING_BATCH_SIZE=100

# File Upload Settings
MAX_UPLOAD_SIZE=2097152 # 2MB

# Database (for production, use PostgreSQL or similar)
# SQLITE is used by default, so no explicit settings here for that.
